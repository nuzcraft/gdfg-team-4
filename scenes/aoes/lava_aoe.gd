extends BaseAOE

func _ready():
	pass

#func _on_body_entered(body):
	#if body.has_method("burn"):
		#body.burn(call_state.Start)
#
#func _on_body_exited(body):
	#if body.has_method("burn"):
		#body.burn(call_state.End)
